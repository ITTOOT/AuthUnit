// CSRF token is typically included as a cookie named "csrftoken" and also
// as a header named "X-CSRFToken" in the request. You need to ensure that
// your frontend code properly sends this token with each request.

// const csrftoken = document.cookie.match(/csrftoken=([^;]+)/)[1];
// // Include the CSRF token in the headers of your HTTP requests
// headers: {
//     'X-CSRFToken': csrftoken
// }

